package model;

public enum SceneType {//using enum java only allows the usage of the words with no typo or wrong casing(this helps the program to not break)

    NORMAL,
    ENDING,
    DEATH,
    ESCAPE

}